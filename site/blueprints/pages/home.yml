Title: Home
sections:
  details:
    type: fields
    fields:
      cta:
        label: Call to Action
        type: structure
        fields:
          event:
            label: Event
            type: select
            options: query
            query: site.index.template('course')
            width: 1/2
          artists:
            label: Artist/s
            type: select
            options: query
            query: site.index.template('artist')
            width: 1/2
